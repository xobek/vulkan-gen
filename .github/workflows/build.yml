name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1.8.0
    - name: Build Engine
      run: nmake /f Makefile.engine.windows.mak
    - name: Build Game
      run: nmake /f Makefile.testbed.windows.mak
